apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.backend.name }}-service
  labels:
    app: {{ .Values.backend.name }}
spec:
  selector:
    app: {{ .Values.backend.name }}
  ports:
  - protocol: TCP
    port: 80
    targetPort: {{ .Values.backend.port }}
  type: ClusterIP